import { counterReducer } from 'entities/Counter/model/slice/counterSlice';
import { Counter } from 'entities/Counter/ui/Counter';
import { ICounterSchema } from './model/types/ICounterSchema';

export {
	Counter,
	counterReducer,
	ICounterSchema,
};
